/**
 * Single toggle for the Reports feature: true serves local mock data,
 * false calls the real backend. Flip this back to false once
 * `/api/v1/report/giornaliero` and `/api/v1/report/mensile` are live.
 */
export const USE_MOCK_REPORT_DATA = true;
